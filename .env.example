# Public site URL (used in metadata, OG tags, sitemap).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# WordPress headless GraphQL endpoint (será preenchido no Prompt 11a).
WP_GRAPHQL_ENDPOINT=

# Google Tag Manager ID (será definido no Prompt 13).
NEXT_PUBLIC_GTM_ID=
